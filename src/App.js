import React, { Component } from "react"
import logo from "./img/logo.svg"
import "./css/App.css"

class App extends Component {
  render() {
    return (
      <div className="App">
		<div id="header">header</div>
		<div id="content">Position the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than thisPosition the middle div using absolute positioning without specifying height. It does not get much simpler than this</div>
		<div id="footer">footer</div>
      </div>
    )
  }
}

export default App
